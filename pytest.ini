[pytest]
# No test classes are used anywhere in this suite -- every test is a plain
# function. bits/test_bit.py's TestBit is a Bit implementation (a durable
# fixture, not a pytest test class), but its name matches pytest's default
# `Test*` collection pattern and triggers a spurious PytestCollectionWarning.
# Disabling class-based collection removes it without renaming TestBit.
python_classes =
